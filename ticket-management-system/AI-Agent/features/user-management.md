# User Management

## Purpose
- Allow admin to create and view users.
- Manage user accounts from admin panel.

## Requirements
- Admin-only access.
- User list view.
- Separate user listing page and create user page.
- New users default to `isAdmin:false`.

## Notes
- Use `/api/users` endpoints.
- Only admin can access this page.
